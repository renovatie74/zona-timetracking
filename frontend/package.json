{
  "name": "zona-timetracking-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:dev": "vite build && wrangler pages deploy dist --project-name timetracking-dev --commit-dirty=true",
    "deploy:prod": "vite build && wrangler pages deploy dist --project-name timetracking-prod --commit-dirty=true",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.24.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.1",
    "vitest": "^4.1.9"
  }
}
